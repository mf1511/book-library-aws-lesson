{
  "name": "parking-application-monorepo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "format": "prettier \"{apps,libs,test}/**/*.{ts,tsx,js,jsx,json}\" --ignore-path .gitignore",
    "format:check": "pnpm format --check",
    "format:write": "pnpm format --write",
    "tsc": "pnpm --parallel -r run tsc",
    "lint": "pnpm --parallel -r run eslint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.6.3"
  }
}
